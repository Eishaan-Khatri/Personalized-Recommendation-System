# Notebooks

The project is implemented as reusable Python modules under `src/`.

Notebook placeholders are listed in the README to show the intended analysis
flow. The command-line pipeline is the source of truth for reproducible metrics:

```bash
python -m src.run_pipeline
```
